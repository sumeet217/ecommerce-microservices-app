apiVersion: v1
kind: Service
metadata:
  name: orders-service
  namespace: ecommerce
  labels:
    app: orders-service
spec:
  selector:
    app: orders-service
  ports:
  - port: 8003
    targetPort: 8003
    protocol: TCP
  type: ClusterIP
