apiVersion: v1
kind: Service
metadata:
  name: cart-service
  labels:
    app: cart-service
spec:
  selector:
    app: cart-service
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
  type: ClusterIP